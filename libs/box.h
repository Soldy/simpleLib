#ifndef BOXH
#define BOXH
char* box(
    const int top,
    const int left,
    const int width,
    const int height
);
char* boxSide(const int width);
char* boxUp(const int width);
char* boxDown(const int width);
#endif
